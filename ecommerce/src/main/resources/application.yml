spring:
  datasource:
    name: ecommercedb
    url: jdbc:h2:mem:ecommercedb

  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
      
  jpa:
    show-sql: true